The Jupyter Notebook `testing-hypotheses-with-ANYI.ipynb` contains a single simple example of how you can load ANYI into Python and interact with it to test hypotheses

The Jupyter Notebook `adding-data-to-ANYI.ipynb` is a work in progress demonstration of how to add data to both ANYI itself and also to the ANYI Browser tool. 
